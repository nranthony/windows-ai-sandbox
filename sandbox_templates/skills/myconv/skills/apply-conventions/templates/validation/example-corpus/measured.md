# example-corpus — measured

*PLACEHOLDER. Regenerated by the harness; never hand-edited. Written by the same command as
`measured.json` — generating them separately is how this table drifts from the data.*

- Generated: YYYY-MM-DD
- Commit: `<short sha of the code that produced this>`
- Command: `<the exact command that regenerates this file>`
- Corpus: `<corpus name and version identifier>`
- Corpus digest: `sha256:<digest of the corpus as used>`

## Summary

| Metric | Measured | Enforced floor | Margin |
|---|---|---|---|
| example_metric | 0.000 | 1.000 (`expected.json`) | −1.000 |
| another_metric | 0.000 | 0.000 (`expected.json`) | 0.000 |

## Per record

| Record | example_metric | another_metric | Notes |
|---|---|---|---|
| `<record-id>` | 0.000 | 0.000 | |

Keep this table reviewable. Past the point where a per-record listing stops being readable as
a diff, report aggregates here and let the harness regenerate the detail on demand.
